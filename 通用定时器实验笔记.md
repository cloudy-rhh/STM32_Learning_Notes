1.通用定时器中断实验：和基本定时器中断实验基本一致，定时器改为TIM2或TIM3即可；
	
2.通用定时器PWM输出实验：
	
	需要自己添加文件（tim.h，tim.c文件），和main.c放在同一级目录下（User层），会调用HAL库内容，要引入HAL库头文件（如果keil项目里未包含，记得手动添加）；
	
	文件内容：
	
	1.定义timX_init函数（一般通用定时器就是TIM2，TIM3，都可以）；
	
	提问：该怎么进行timX_init函数体的编程呢？
	
	步骤： 1.定义一个GPIO结构体（局部变量）：GPIO_InitTypeDef gpio_initstruct（GPIO实验中有）；
		   
		    2.使能TIMX外设时钟： __HAL_RCC_TIM2_CLK_ENABLE（）;
		   
		    3.调用时钟使能宏：__HAL_RCC_GPIOX_CLK_ENABLE（）（X根据要调用按键的引脚组别来决定）（一般通用定时器有好几个CH通道，随便选一个即可，具体查原理图）；
		    
		    4.引脚结构体成员赋值：gpio_initstruct.Pin，确定引脚参数；
						   gpio_initstruct.Mode，确定输入输出模式参数（复用推挽输出用GPIO_MODE_AF_PP）；
						   gpio_initstruct.Pull，确定输入模式类型参数（上拉输入用GPIO_PULLUP，下拉输入用GPIO_PULLDOWN）；
		 				   gpio_initstruct.Speed，输出速度参数（一般意义不大，写GPIO_SPEED_FREQ_LOW）；
		   
		    5.调用初始化执行函数（HAL库的函数）：
		   
		    函数用法：HAL_GPIO_Init（引脚的宏,  对应GPIO结构体的地址），将配置直接写入硬件寄存器；
		   
		  （以上步骤是在配置PWM信号输出端口，由于没有LED灯并非该端口，需用杜邦线来帮助LED灯接收信号变化）；
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------
		    6.定义一个TIM结构体（全局变量）：TIM_HandleTypeDef g_tim2_handle（TIM_HandleTypeDef是一个定时器结构体，包含定时器参数）；
		    7.定时器结构体成员赋值：g_tim2_handle.Instance，确定时钟号；
		   					      g_tim2_handle.Init.Prescaler，确定预分频器（PSC，用于降低系统时钟频率）（计算公式：计数频率 = 系统时钟频率 /（PSC+1））；
							      g_tim2_handle.Init.CounterMode，确定定时器模式（TIM_COUNTERMODE_UP代表向上计数（从0数到ARR），						TIM_COUNTERMODE_DOWN代表向下计数（从ARR数到0））；
							      g_tim2_handle.Init.Period，确定自动重装载值（ARR，确定计数器数到多少次更新一次事件）（从0开始，实际次数为ARR+1）；
							      g_tim2_handle.Init.AutoReloadPreload ，启用预装载功能（TIM_AUTORELOAD_PRELOAD_ENABLE代表启用，						TIM_AUTORELOAD_PRELOAD_DISABLE代表不启用，启用更加安全）；
		   8.写入硬件寄存器： HAL_TIM_PWM_Init(&g_tim2_handle)（基本中断功能就用Base，PWM用PWM功能）;
		   （以上步骤是在初始化通用定时器，由于是使用PWM功能，和Base参数略有不同）；
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------
		   9.设置PWM通道：
		   步骤：1.定义一个通道结构体：TIM_OC_InitTypeDef oc_config（和GPIO，定时器结构体同理）；
			      2.设置PWM模式：oc_config.OCMode（PWM1 模式下，当定时器计数器（CNT）小于比较值（CCR）时，输出有效电平（高电平或低电平，取决于极性设	置）；当 CNT 大于或等于 CCR 时，输出无效电平）；
    			      3.设置比较值（CCR）：oc_config.Pulse（占空比 = CCR / (ARR + 1））；
			      4.设置输出极性：oc_config.OCPolarity（PWM输出高电平时LED灯亮，用TIM_OCPOLARITY_HIGH，反则使用TIM_OCPOLARITY_LOW，具体看硬件配	置）；
			      5.设置是否需要快速模式：oc_config.OCFastMode（一般不用，使用TIM_OCFAST_DISABLE）；
			      6.将配置写入硬件： HAL_TIM_PWM_ConfigChannel(&g_tim2_handle, &oc_config, TIM_CHANNEL_1)；
  			      说明：函数用法： HAL_TIM_PWM_ConfigChannel（定时器句柄，包含通道配置的结构体，要配置的通道号）；
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------
       		   10.启动定时器PWM输出：调用HAL_TIM_PWM_Start（&g_tim2_handle, TIM_CHANNEL_1）（和GPIO的区别在于，GPIO实验在HAL_GPIO_Init中就会自动根据模式使能中断函数，而定时器实验则需要手动调用函数(启动内部计时器））。

	2.main函数使用：
	1.调用初始化函数timX_init（）；
	2.设置LED亮度变化逻辑：
		1.设置一个变量duty（初始值设为0）；
		2.使用for循环，调用函数__HAL_TIM_SET_COMPARE来实时修改占空比：
		 for (duty = 0; duty < 1000; duty += 10)
			{
					__HAL_TIM_SET_COMPARE(&g_tim2_handle, TIM_CHANNEL_1, duty);
					HAL_Delay(30);      //控制软件变化速度，方便人眼观察
			}
				 for (duty = 1000; duty > 0; duty -= 10)
			{
					__HAL_TIM_SET_COMPARE(&g_tim2_handle, TIM_CHANNEL_1, duty);
					HAL_Delay(30); 
			}
    		函数用法：_HAL_TIM_SET_COMPARE（定时器句柄指针，修改通道，新的占空比）
最后可以观察到LED亮度发生变化，实验成功，通过PWM控制LED灯亮度；

3.通用定时器输入捕获实验：
	需要自己添加文件（tim.h，tim.c文件），和main.c放在同一级目录下（User层），会调用HAL库内容，要引入HAL库头文件（如果keil项目里未包含，记得手动添加）；
	文件内容：
	1.配置TIM3的PWM输出（上一个实验有，TIM2改为TIM3即可）；
	2.配置TIM2的PWM按键输入：
	步骤：1.配置引脚参数（之前的实验里有讲，照着配，模式应为GPIO_MODE_AF_INPUT，复用输入模式，以此接收信号）；
		   2.配置定时器参数（将ARR改为0xFFFF，防溢出，再将硬件写入函数改为HAL_TIM_IC_Init，其他基本不变）（HAL_TIM_IC_Init会额外初始化定时器的输入捕获功	能，这是关键的区别）；
		   3.配置捕获通道：TIM_IC_InitTypeDef ic_config，定义一个捕获通道结构体；
					        ic_config.ICPolarity，一般捕获上升沿（TIM_ICPOLARITY_RISING）（当 PA0 检测到信号从低电平跳变到高电平时，触发一次捕获事件，记录当	前计数器的值）；
						ic_config.ICSelection，一般常用直连通道（TIM_ICSELECTION_DIRECTTI）；
						ic_config.ICPrescaler，这里选择不分频（TIM_ICPSC_DIV1，即每检测到一个边沿就触发一次捕获）（DIV2则是每两个边沿触发一次）；
						ic_config.ICFilter，决定是否选择滤波器（这里不需要，直接写0即可）；
						HAL_TIM_IC_ConfigChannel(&g_tim2_handle, &ic_config, TIM_CHANNEL_1)，程序写入硬件；
		    4.设置中断优先级（和之前实验同理）：HAL_NVIC_SetPriority(TIM2_IRQn, 1, 0);
						    HAL_NVIC_EnableIRQ(TIM2_IRQn);
		    5.使能中断，启动输入捕获（和之前实验同理，但要改函数名称和用法）：HAL_TIM_IC_Start_IT(&g_tim2_handle, TIM_CHANNEL_1);
	3.重写回调函数（中断函数怎么在硬件上响应，怎么添加之前的实验里有，这里不多说明，只说明回调函数本身）：
		1.设置3个变量，实现一个简单状态机：capture_val1，capture_val2，capture_cnt，都初始化为0（capture_val1存储第一次捕获到的计数器值，capture_val2存储	第二次捕获到的计数器值，capture_cnt为状态标志）；
		2.	void HAL_TIM_IC_CaptureCallback(TIM_HandleTypeDef *htim)（函数名必须写这个）
			{
    				if (htim->Instance == TIM2)
    				{
        				if (capture_cnt == 0) //第一次捕获
        				{
            					capture_val1 = HAL_TIM_ReadCapturedValue(htim, TIM_CHANNEL_1);
            					capture_cnt = 1; 
       			 		}
     				        else if (capture_cnt == 1) //第二次捕获
       				        {
          				        capture_val2 = HAL_TIM_ReadCapturedValue(htim, TIM_CHANNEL_1);
            					capture_cnt = 0;

           					uint32_t period = (capture_val2 - capture_val1) & 0xFFFF; //利用捕获值差值记录一个完整周期的计数值，& 0xFFFF用于防溢出            					
						uint32_t freq = 1000000 / period; // 计数频率 1MHz，period就是周期，且单位是微妙（10-6次方秒），以此来算周期

            					printf("Period: %d us, Freq: %d Hz\r\n", period, freq); //打印捕获信息
       					 }	
   				 }
			}
		说明： HAL_TIM_ReadCapturedValue(htim, TIM_CHANNEL_1)用于返回CNT中的计数值，将其锁存到CCR寄存器中，再根据计数器频率来推算时间；

	main函数使用：
	调用两个初始化函数，再初始化串口即可，可以在主循环里写程序确认系统正常运行。

4.通用定时器脉冲计数实验：
	需要自己添加文件（tim.h，tim.c文件），和main.c放在同一级目录下（User层），会调用HAL库内容，要引入HAL库头文件（如果keil项目里未包含，记得手动添加）；
	文件内容：
	1.定义计数函数；

	问题：该怎么进行计数函数主体编程？
	步骤：1.定义一个GPIO结构体；
		   2.定义一个TIM结构体；
		   3.进行GPIO引脚配置，结构体参数配置（引脚配置看原理图，模式选择GPIO_MODE_AF_INPUT）；
		   4.进行TIM结构体参数配置（不分频，PSC为0，且ARR设为最大0xFFFF）；
		  （上面步骤之前实验里多次用到，除特别提出的参数外，其他参数基本保持不变，不多讲解）
		   5.配置外部时钟源：定义一个时钟源结构体：TIM_ClockConfigTypeDef sClockSourceConfig = {0}；
		   ClockSource，设置计数时钟来源模式（采用外部触发输入ETR，模式1，TIM_CLOCKSOURCE_ETRMODE1）；
		   ClockPolarity，设置有效触发边沿（TIM_CLOCKPOLARITY_NONINVERTED检测上升沿，TIM_CLOCKPOLARITY_INVERTED检测下降沿，具体选什么查看原理	图）；
		   ClockPrescaler，设置ETR分频系统（实验中通常设为TIM_CLOCKPRESCALER_DIV1，即每个脉冲都计数，不分频）；
		   ClockFilter，设置 ETR 引脚输入的硬件滤波长度（设置为0，不进行滤波）；
		   HAL_TIM_ConfigClockSource(&g_tim2_handle, &sClockSourceConfig)，将代码写入硬件；
		   6.配置定时器“从模式”：定义一个从模式结构体：TIM_SlaveConfigTypeDef sSlaveConfig = {0}；
		   SlaveMode，设置定时器从模式（设置为外部触发模式1，TIM_SLAVEMODE_EXTERNAL1）；
		   InputTrigger，指定触发信号源（设置为ETR引脚输入，TIM_TS_ETRF）；
		   HAL_TIM_SlaveConfigSynchro(&g_tim2_handle, &sSlaveConfig)，将代码写入硬件；
		   7.启用功能： HAL_TIM_Base_Start(&g_tim2_handle)（启动计数器）。

	main函数使用：
	1.初始化串口；
	2.调用计数器初始化函数；
	3.函数主体编写（计数）：
	while(1)
    	{ 
		uint16_t cnt = __HAL_TIM_GET_COUNTER(&g_tim2_handle);
        	printf("Pulse count: %d\r\n", cnt);
        	HAL_Delay(500);
         }
	说明：__HAL_TIM_GET_COUNTER用于读取定时器当前计数器的值（CNT 寄存器）。
	