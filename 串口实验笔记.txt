串口通信实验
	情况1：有已定义的usart文件
	不需要创建新文件，直接调用已有文件函数；

	1.初始化阶段一般调用usart_init函数：
	函数用法：
	usart_init（波特率）（波特率一般定为115200，可调）；

	2.main函数使用：
	直接在while循环中写入printf（和C语言用法一致），再写入delay_ms（加大信息发送间隔）；
	说明：printf函数可以调用是因为usart文件中的fputc函数。

	情况2：没有已定义的usart文件
	需要创建新的文件（usart.h，usart.c文件），和main.c放在同一级目录下（User层，方便使用），要记得调用HAL库；
	usart文件内容：
	1.定义串口初始化函数usart_init；

	提问：该怎么进行usart_init函数体的编程呢？
	步骤：1.定义一个UART结构体（全局变量）：UART_HandleTypeDef g_uart1_handle（UART_HandleTypeDef是一个结构体类型，包含串口参数）；
		   2.定义一个GPIO结构体（局部变量）：GPIO_InitTypeDef gpio_initstruct（GPIO_InitTypeDef是一个结构体类型，包含GPIO配置参数）;
		   3.调用一个USART时钟使能宏： __HAL_RCC_USARTX_CLK_ENABLE()（X默认为1，可以根据需要修改）；
		   4.调用时钟使能宏：__HAL_RCC_GPIOX_CLK_ENABLE() （X根据要调用按键的引脚组别来决定）；
		   5.配置TX引脚（输出引脚）（步骤和配key的引脚类似，引脚号查原理图）（模式改为GPIO_MODE_AF_PP，速度最后改为HIGH）；
		   6.配置RX引脚（输入引脚）（同上）（模式改为GPIO_MODE_AF_INPUT，速度最后改为HIGH）；
		   7.配置串口参数：g_uart1_handle.Instance ，确定串口外设（一般默认为USART1）；
					       g_uart1_handle.Init.BaudRate ，确定波特率（波特率会通过函数传递进来）；
					       g_uart1_handle.Init.WordLength ，确定数据位宽度（一般每个数据包用8B表示一个字节，即UART_WORDLENGTH_8B）；
					       g_uart1_handle.Init.StopBits ，设置停止位长度（一般就是1bit，即UART_STOPBITS_1）；
					       g_uart1_handle.Init.Parity ，确定校验位（不加校验，传输速度快，用UART_PARITY_NONE）；
					       g_uart1_handle.Init.Mode，确定工作模式（可以同时启用发送和接收，也可以只用其中之一）（同时启用为UART_MODE_TX_RX）；
					       g_uart1_handle.Init.HwFlowCtl，设置硬件流控（一般设为UART_HWCONTROL_NONE）；
					       g_uart1_handle.Init.OverSampling ，设置过采样（一般设为UART_OVERSAMPLING_16，提高抗干扰能力）；
                   8.调用HAL_UART_Init(&g_uart1_handle)（写入硬件）。

	2.重写fputc函数：
	写法（直接照搬）（保障printf函数使用）：
	int fputc(int ch, FILE *f)
	{
    		HAL_UART_Transmit(&g_uart1_handle, (uint8_t *)&ch, 1, 0xFFFF);
   	 	return ch;
	}
	函数说明：
	HAL_UART_Transmit是发送数据的函数；
	HAL_UART_Transmit（串口号，发送数据指针，要发送的字节数，超时时间）（0xFFFF大约有65秒，可视为一直等待）。

	3.main函数使用：
	和情况1使用方法一致；
